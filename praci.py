#second largest digit of the number

'''x = str(abs(int(input("Enter the number:"))))

largest = 0
seclargest = -1

for  ch in x:
    if   "0" <=    ch   <= "9":
        digit = int(ch)
        if digit > largest:
            largest = digit
    

            
for ch in x:
    if "0" <= ch <="9":
        digit = int(ch)
        if digit < largest and digit > seclargest:
            seclargest = digit
        

print(seclargest)'''
            

    
            
